apiVersion: apps/v1
kind: Deployment
metadata:
  name: mqtt-deployment
  labels:
    app: mqtt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mqtt
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: mqtt
    spec:
      containers:
        - name: mqtt-broker
          image: eclipse-mosquitto
          ports:
            - containerPort: 1883
      restartPolicy: Always
      volumes: null
---
apiVersion: v1
kind: Service
metadata:
  name: mqtt-service
  labels:
    app: mqtt
spec:
  ports:
    - name: "mqtt"
      port: 1884
      targetPort: 1883
      protocol: TCP
  selector:
    app: mqtt
  type: NodePort
